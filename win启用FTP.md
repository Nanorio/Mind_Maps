### 1.启用windows的IIS组件服务

### 2.在windows上新建一个专门用于访问ftp的账户,必须要有密码,否则无法登录

### 2.重新设置新站点FTP(工作目录,权限,账户)

### 3.右键站点绑定本地网卡IP地址(可绑定多个本机ip地址,允许虚拟网卡,本地回环地址等)

### 4.设置允许/禁止源ip访问规则(为了安全)

### 5.可设置外部(物理/虚拟)防火墙,本质上就是额外设置一个出入站网关,如果设置了防火墙,则ftp的访问必须经过该网关

### 6.指定默认域



<hr />

⭐注意事项<br />
1.如果本机访问本机,那么用户名前面就必须加".",比如输入用户名:.\\ftp,密码:1234<br />
2.当然你也可以使用匿名方式,省事,只要配置显示允许/禁止ip访问规则即可

