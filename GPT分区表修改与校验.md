GPT磁盘分区表修改


### 找到分区表项
1. 跳转到扇区(512)2,也就是LBA2，这里就是分区表的具体内容
2. 每128byte一个分区表项，找到要修改的分区表项

### 修改分区表项
对分区表项,你可以进行如下操作
> + 全部清0该分区表项,使得该分区不再被读取
> + 修改分区表的起始位置(由于物理扇区大小是4K,所以起始位置最好是4096byte的整数倍,比如0x4000,0x10000)和结束位置(结束位置不限)<br />这就叫做4K对齐,可以提高硬盘读取速度
> + 修改分区的名字/GUID
> + 修改分区的属性值(位于分区表项0x30~0x37,共64bit):不同分区定义的属性值不同,bit48~55,关于安卓a/b切槽,boot_a/b的分区表项或许这是关键

### 更新CRC32校验值，而且有2个校验值，都在GPT头(LBA1即扇区1)<br />
不正确校验磁盘,操作系统就拒绝解码其中的内容,甚至会让你重新格式化磁盘<br />
必须按照以下顺序进行校验值的修改,因为第1个校验值会被第2个校验值所校验，两者是依赖而非独立关系
1.  对所有分区表项的校验，即分区总表的CRC32校验，该校验值位置为GPT表头即LBA1的0x58~0x5B
> + 把分区表即LBA2~LBA33(即扇区2-扇区33)的所有数据16进制Hex值复制下来,小端序(低位在前高位在后)排列,使用工具把这串32*512*2=32768个数字进行CRC32校验
> + 得出的数值也是16进制Hex值,对应0x58~0x5B,改进去,注意低位对低位，比如数值是AA6D4092,那么0x58就是92,0x59就是40,不够就补0
2.  对GPT头进行CRC32校验,也就是对LB1的0x00~0x5C这固定92个byte进行CRC32校验，该校验值位置为GPT表头即LBA1的0x10~0x13
> + 把这92个byte的数据按照上面的方法复制下来,
> + 再把0x10~0x13全部替换为0,然后得到184个数字给工具CRC32校验,改进对应位置,同上,注意低位对低位,到这一步就完成了
> + 为什么要把0x10~0x13清0？因为校验的时候自己不能对自己校验,用于禁止自指现象

### 更新"备份GPT头"
在磁盘最后一个扇区就是GPT头的备份,你需要在原有"备份GPT头"上重新GPT分区表CRC校验值和"备份GPT头"CRC校验值,其余一般别乱动

### 更新备份分区表项
在磁盘最后也有32块扇区是备份分区表,把修改好的分区表也复制到该备份分区表,让两者数据一致
> + 备份分区表起始位置为(最后的扇区号-32)号扇区,比如我最后一个扇区号是1000215215,那备份分区分区表的起始位置就是(1000215215-32=1000215183)号扇区


⭐相关工具(Windows)
GPT扇区数据编辑/备份/恢复：DiskGenius

⭐相关工具(Linux)
sgdisk(安卓大部分自带)

⭐关键词
GPT 分区表 安卓 win 备份 恢复 slot A/B VAB 分区属性值

⭐在修改分区表之前，务必完整备份"GPT头，分区表，备份分区表，备份GPT头"四个部分的完整16进制原始数据,软件的备份不通用也不靠谱



